# Tests

Tests should cover critical application behaviors such as navigation flow, auth handling, localization behavior, settings persistence, and data access logic. Keep mocks and test utilities centralized here.

Feature-specific tests can live beside the feature code when that makes ownership clearer.
