# Profile Feature

The profile feature owns the main Profile tab screen.

## Main pieces

- `screens/ProfileScreen`: profile route UI composition
- `index.ts`: public export for navigation

The screen composes shared profile UI components and currently uses `useProfileViewModel` from the shared view model layer.
