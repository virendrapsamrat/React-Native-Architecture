# Utilities

Utility helpers belong here when they are small, reusable across the app, and not owned by a single feature. Examples include validation, formatting, date helpers, logout coordination, and app-level storage utilities.

Avoid placing UI behavior or feature workflows here; those belong in screens, hooks, view models, or feature modules.
