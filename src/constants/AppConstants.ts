export const AppConstants = {
  APP_NAME: 'RN Architecture App',
  APP_VERSION: '1.0.0',
  DEFAULT_LANGUAGE: 'en',
  SUPPORTED_LANGUAGES: ['en', 'hi', 'te'] as const,
  API_TIMEOUT: 30000,
  DEBOUNCE_DELAY: 300,
  PAGINATION_LIMIT: 20,
} as const;
