# Constants

Shared constants live here to keep styling, storage keys, regex patterns, supported language lists, and app values consistent across the codebase.

Do not put translated display labels here; use `src/localization` instead.
