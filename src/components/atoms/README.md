# Atoms

Atoms are the smallest reusable UI elements. They should stay simple, focused, and stateless whenever possible.

Current atoms include `Avatar`, `Button`, `Icon`, `Loader`, and `Text`. Prefer these wrappers over raw primitives when the app already has a shared style or theme behavior for that element.
