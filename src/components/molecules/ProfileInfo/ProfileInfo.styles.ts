import { StyleSheet } from 'react-native';

export const profileInfoStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    borderRadius: 8,
    borderWidth: 1,
    flexDirection: 'row',
    gap: 12,
    padding: 16,
    width: '100%',
  },
  info: {
    flex: 1,
  },
});
