import { StyleSheet } from 'react-native';

export const profileInfoStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flexDirection: 'row',
    gap: 12,
  },
  info: {
    flex: 1,
  },
});
