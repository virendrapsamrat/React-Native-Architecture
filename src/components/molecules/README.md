# Molecules

Molecules combine a few atomic components into a small, reusable unit such as a field block, profile row, search input, or compact statistic.

Current molecules include `FormField`, `ProfileInfo`, `SearchBar`, and `UserStat`.
