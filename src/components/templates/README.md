# Templates

Templates provide page-level layouts and composition patterns for screens. They help keep screen code consistent while allowing screen-specific content to vary.

Current templates include `MainTemplate` and `ProfileTemplate`. Templates should handle layout structure, spacing, and slots, not feature business logic.
