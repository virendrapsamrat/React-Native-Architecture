# Organisms

Organisms are larger UI sections that group multiple molecules and atoms into a coherent experience.

Current organisms include `ProductCard`, `ProfileHeader`, `ProfileStats`, and `SettingsSection`. Keep them reusable; if an organism becomes tightly coupled to one feature's state, move that logic into the feature screen or feature hooks.
