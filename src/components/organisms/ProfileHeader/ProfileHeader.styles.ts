import { StyleSheet } from 'react-native';

export const profileHeaderStyles = StyleSheet.create({
  container: {
    padding: 20,
    alignItems: 'center',
  },
  bio: {
    marginTop: 12,
    textAlign: 'center',
  },
});
