import { StyleSheet } from 'react-native';

export const profileHeaderStyles = StyleSheet.create({
  bio: {
    marginTop: 12,
    textAlign: 'center',
  },
  container: {
    alignItems: 'center',
    padding: 20,
  },
  name: {
    marginTop: 12,
  },
});
